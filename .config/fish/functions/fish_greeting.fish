function fish_greeting -d "What's up, fish?"
    # this overwrites: ~/.local/share/omf/themes/bobthefish/fish_greeting.fish
    # set_color $fish_color_autosuggestion
    # echo "waketime:" (sysctl -a |grep waketime |cut -d " " -f 10-15)

    # echo "node" (node --version)
    # echo "npm" (npm --version)

    # set_color normal
end